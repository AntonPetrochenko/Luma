import { Vector } from 'ts-matrix'

/** This type holds coordinates in 1/32ths of a block as used by the Classic Protocol */
type FractionPosition = number;
/** This type holds coordinates in whole blocks */
type BlockPosition = number;


export interface Entity {
  
}